package com.example.miraj.blakittest.activity;

import android.support.v4.app.Fragment;

public interface UpdateFragmentListener {
    void updateFragment(Fragment fragment);
    void updateFragmentWithBackStack(Fragment fragment);
}
